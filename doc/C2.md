```plantuml
@startuml C4_Elements
!include https://raw.githubusercontent.com/plantuml-stdlib/C4-PlantUML/master/C4_Container.puml

Person(utilisateur, "Utilisateur", "Un employé de l'entreprise")
Person(admin, "Administrateur", "Le patron de l'entreprise")
Person(visiteur, "Visiteur", "Une personne pas reconnue par l'application")
Boundary(app, "Application System") {
  ContainerDb(db, "Database", "La base de donnée mysql pour l'application")
  Container(api, "REST API", "API REST construite avec FastAPI")
  Container(frontend, "Frontend", "Frontend construit avec vue.js")

  Rel(frontend, api, "Appelle à l'API", "Json/HTTPS")
  Rel(api, db, "Interroger la BDD", "ORM")
}
Rel(utilisateur, frontend, "")
Rel(admin, frontend, "")
Rel(visiteur, frontend, "")
@enduml
```
